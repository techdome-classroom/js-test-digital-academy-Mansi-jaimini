/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    const s=[];
    for(let i=0;i<s.length;i++){
        const top=s[s.length-1]
    }
    if(s[i]==='(' )
    
};

module.exports = { isValid };


